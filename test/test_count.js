'use strict'

var expect = require('chai').expect
var count = require('../counter/count')

describe ('a counter function', function() {
  it('should start the count at zero', function(done) {

    expect(value.currCount).to.eql(0)
    done()

  })
})
